{{- if and .Values.edgeSecret.value (not .Values.edgeSecret.existingSecret) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ohmywind-mcp.fullname" . }}
  labels:
    {{- include "ohmywind-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  OPENWIND_EDGE_SECRET: {{ .Values.edgeSecret.value | quote }}
{{- end }}
